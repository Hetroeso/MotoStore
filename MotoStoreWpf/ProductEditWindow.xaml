<Window x:Class="MotoStoreWpf.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование товара" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Название:"/>
        <TextBox x:Name="NameTextBox" Grid.Row="0" Margin="100,0,0,5" Width="250"/>

        <Label Content="Цена:" Grid.Row="1"/>
        <TextBox x:Name="PriceTextBox" Grid.Row="1" Margin="100,0,0,5" Width="100"/>

        <Label Content="Категория:" Grid.Row="2"/>
        <TextBox x:Name="CategoryTextBox" Grid.Row="2" Margin="100,0,0,5" Width="250" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

        <Button Content="Сохранить" Grid.Row="4" HorizontalAlignment="Right" Width="100"
                Click="SaveButton_Click" Margin="0,10,0,0"/>
    </Grid>
</Window>
